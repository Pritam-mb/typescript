const superhero : string[]=[];
//or
const myhero: Array<number>=[];

type user={
    name: string,
    age: number,
    islogin: boolean
}

const hero : user[]=[]
hero.push({
    name: "gopal",
    age: 25,
    islogin: true
})

console.log(hero);

superhero.push('spiderman')