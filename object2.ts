const superhero : string[]=[];
//or
const myhero: Array<number>=[];

type user={
    name: string,
    age: number,
    islogin: boolean
}

const hero : user[]=[]
hero.push({
    name: "gopal",
    age: 25,
    islogin: true
})

console.log(hero);


superhero.push('spiderman')

let alluser :user[]=[]

const mlmodels: number[][]=[]
mlmodels.push([255,255,255])

export{};